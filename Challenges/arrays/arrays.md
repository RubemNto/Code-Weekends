```
1. Given an array of Integers, sort the array upwards/downwards and return it

EXAMPLE:
[1,4,2,6,5]
return [1,2,4,5,6] or [6,5,4,2,1]

2. Given an array of size n and a new element that respects the array elements type, add to the end of that this and return it 

EXAMPLE:
[1,2,4,5,6] + [5]
return [1,2,3,5,6,5]

3. Given an array of integers of size n, find the last missing element of the sequence. 

EXAMPLE:
1,2,4,5,6
return 3
```